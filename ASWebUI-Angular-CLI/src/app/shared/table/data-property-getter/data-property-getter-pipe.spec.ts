import { DataPropertyGetterPipe } from './data-property-getter-pipe';

describe('DataPropertyGetterPipe', () => {
  it('should create an instance', () => {
    expect(new DataPropertyGetterPipe()).toBeTruthy();
  });
});
