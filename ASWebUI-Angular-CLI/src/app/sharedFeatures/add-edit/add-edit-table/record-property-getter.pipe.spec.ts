import { RecordPropertyGetterPipe } from './record-property-getter.pipe';

describe('RecordPropertyGetterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordPropertyGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
